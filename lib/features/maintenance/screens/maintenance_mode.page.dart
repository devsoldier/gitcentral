import 'package:flutter/material.dart';

class MaintenanceModePage extends StatelessWidget {
  const MaintenanceModePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('We are under maintenance'),
    );
  }
}
